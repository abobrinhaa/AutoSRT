# Chaves e configuração local (NUNCA versionar)
config.json
.env

# Backups gerados pelo próprio AutoSRT
*_backup.srt

# Build do PyInstaller. O executável é publicado nos Releases do GitHub
# (.github/workflows/build-release.yml), não versionado aqui: um binário de
# 13+ MB no histórico do Git nunca sai de lá, mesmo trocado de versão.
build/
dist/
*.spec
AutoSRT.exe

# Python
__pycache__/
*.py[cod]

# Pasta de trabalho do servidor web (filmes e legendas do usuário)
midia/
